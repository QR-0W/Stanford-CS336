# @package _global_

paths:
  # Set to your data, tokenized with GPT-2 tokenizer!
  train_bin: /mdata/wjx/CS336/assignment4-data/data/filtered_one_wet_v2/tokenized.bin
  
  # The validation set which we have already tokenized with GPT-2 tokenizer, don't change this!
  valid_bin: /mdata/wjx/CS336/assignment4-data/data/filtered_one_wet_v2/valid.bin
  
  model_output: output/your_data

training:
  wandb_entity: null  # Set to your wandb entity, e.g. pliang
  wandb_project: null
